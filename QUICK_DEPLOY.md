# ⚡ Быстрый деплой на Railway

## 🚀 Пошаговая инструкция:

### 1. Убедитесь, что код в GitHub
```bash
git add .
git commit -m "Ready for Railway"
git push origin main
```

### 2. Создайте проект на Railway
1. Откройте https://railway.app
2. Войдите через GitHub
3. **New Project** → **Deploy from GitHub repo**
4. Выберите ваш репозиторий

### 3. Добавьте переменные окружения
В Railway: **Settings** → **Variables** → **New Variable**

Добавьте:
- `BOT_TOKEN` = ваш токен от @BotFather
- `ADMIN_ID` = `1226518807`
- `WEBHOOK_URL` = (оставьте пустым пока, добавите после деплоя)

### 4. Дождитесь деплоя
Railway автоматически начнет деплой. Дождитесь завершения.

### 5. Получите домен и установите вебхук
После деплоя:
1. **Settings** → **Networking** → **Generate Domain**
2. Скопируйте URL (например: `https://tower-bot-production.up.railway.app`)
3. Обновите переменную `WEBHOOK_URL` = `https://your-domain.railway.app/webhook`
4. Или откройте в браузере:
   ```
   https://your-domain.railway.app/set_webhook?url=https://your-domain.railway.app/webhook
   ```

### 6. Готово! 🎉
Проверьте бота в Telegram - отправьте `/start`

## ✅ Чеклист перед деплоем:
- [ ] Код запушен в GitHub
- [ ] `BOT_TOKEN` готов
- [ ] `railway.json` есть в проекте
- [ ] `Procfile` есть в проекте
- [ ] `requirements.txt` есть в проекте
- [ ] `runtime.txt` есть в проекте

## 🔍 Проверка работы:
- Логи: Railway → View Logs
- Health check: `https://your-domain.railway.app/health`
- Тест бота: `/start` в Telegram
