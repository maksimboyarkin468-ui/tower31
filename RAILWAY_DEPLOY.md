# 🚀 Деплой на Railway

## Шаги для деплоя:

### 1. Подготовка репозитория
Убедитесь, что все файлы закоммичены и запушены в GitHub:
```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### 2. Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий `tower_bot`

### 3. Настройка переменных окружения

В настройках проекта (Settings → Variables) добавьте:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `BOT_TOKEN` | Ваш токен бота | Токен от @BotFather |
| `ADMIN_ID` | `1226518807` | ID администратора |
| `PORT` | `5000` | Порт (Railway назначит автоматически) |
| `WEBHOOK_URL` | `https://your-app.railway.app/webhook` | URL вебхука (после деплоя) |

**Важно:** `WEBHOOK_URL` нужно будет установить ПОСЛЕ первого деплоя, когда Railway выдаст вам домен.

### 4. Настройка домена и вебхука

После первого деплоя:

1. В Railway перейдите в Settings → Networking
2. Нажмите "Generate Domain" или используйте свой домен
3. Скопируйте полученный URL (например: `https://tower-bot-production.up.railway.app`)
4. Обновите переменную `WEBHOOK_URL` на: `https://your-domain.railway.app/webhook`
5. Или установите вебхук вручную через браузер:
   ```
   https://your-domain.railway.app/set_webhook?url=https://your-domain.railway.app/webhook
   ```

### 5. Проверка деплоя

1. Проверьте логи в Railway (View Logs)
2. Убедитесь, что нет ошибок
3. Проверьте health endpoint: `https://your-domain.railway.app/health`
4. Протестируйте бота в Telegram

### 6. Структура файлов для Railway

Убедитесь, что в проекте есть:
- ✅ `railway.json` - конфигурация Railway
- ✅ `Procfile` - команда запуска
- ✅ `requirements.txt` - зависимости Python
- ✅ `runtime.txt` - версия Python (3.11.0)
- ✅ `bot.py` - основной файл бота
- ✅ `database.py` - работа с БД
- ✅ `config.py` - конфигурация

### 7. Автоматический деплой

Railway автоматически деплоит при каждом push в основную ветку (main/master).

### 8. Проверка работы бота

После деплоя:
1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте все функции:
   - Получение сигнала
   - Проверка подписки
   - Панель администратора

## 🔧 Устранение проблем

### Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что `WEBHOOK_URL` установлен правильно
- Проверьте, что токен бота правильный

### Ошибки с Python версией
- Убедитесь, что `runtime.txt` содержит `python-3.11.0`
- Проверьте переменную `PYTHON_VERSION` в Railway

### База данных не работает
- Railway создаст новую БД при каждом деплое (эпиhemeral storage)
- Для постоянного хранения используйте Railway PostgreSQL или внешнюю БД

## 📝 Примечания

- Railway предоставляет бесплатный план с ограничениями
- База данных SQLite будет сбрасываться при каждом редеплое (используйте PostgreSQL для постоянного хранения)
- Домен Railway может меняться при редеплое (используйте свой домен для стабильности)
