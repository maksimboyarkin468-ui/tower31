# 📚 Полная инструкция по GitHub - от создания до обновления

## 🎯 Часть 1: Первая загрузка кода на GitHub

### Шаг 1: Установка Git (если еще не установлен)

1. Скачайте Git: https://git-scm.com/download/win
2. Установите Git (все настройки по умолчанию - просто "Next")
3. Проверьте установку: откройте PowerShell/CMD и введите:
   ```bash
   git --version
   ```
   Должно показать версию Git (например: `git version 2.43.0`)

### Шаг 2: Создание репозитория на GitHub.com

1. Зайдите на [github.com](https://github.com)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name:** `tower-bot` (или любое имя)
   - **Description:** `Telegram bot for Tower Rush signals` (опционально)
   - **Public/Private:** Выберите сами (это НЕ влияет на доступность бота!)
   - **Add .gitignore:** Выберите **Python** (или None, у нас уже есть)
   - **Add a license:** Можно выбрать MIT или оставить None
   - ❌ **НЕ ставьте** галочку "Initialize with README" (у нас уже есть файлы)
5. Нажмите **"Create repository"**

### Шаг 3: Настройка Git на компьютере (первый раз)

Откройте PowerShell или CMD в папке проекта:

```bash
cd C:\Users\boiar\tower_bot
```

Настройте Git (сделайте один раз):
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

Замените на ваши данные! Это будет видно в истории коммитов.

### Шаг 4: Инициализация репозитория

В папке `C:\Users\boiar\tower_bot` выполните:

```bash
# 1. Инициализируйте Git репозиторий
git init

# 2. Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# 3. Проверьте, что будет загружено (опционально)
git status
```

**Что показывает `git status`:**
- ✅ Зеленым - файлы готовы к загрузке
- ⚠️ Красным - файлы в .gitignore (НЕ будут загружены)

### Шаг 5: Первый коммит (сохранение изменений)

```bash
git commit -m "Initial commit: Tower Bot"
```

`-m` - это сообщение коммита (описание изменений)

### Шаг 6: Подключение к GitHub

Получите URL вашего репозитория на GitHub:
1. Зайдите на страницу вашего репозитория на GitHub
2. Нажмите зеленую кнопку **"Code"**
3. Скопируйте URL (например: `https://github.com/yourusername/tower-bot.git`)

В PowerShell выполните:
```bash
git remote add origin https://github.com/yourusername/tower-bot.git
```

Замените `yourusername` на ваш GitHub логин!

### Шаг 7: Создание основной ветки

```bash
git branch -M main
```

Это создает ветку `main` (главная ветка)

### Шаг 8: Загрузка на GitHub

```bash
git push -u origin main
```

**Что происходит:**
- Git загружает все файлы на GitHub
- `-u origin main` - запоминает, куда загружать в будущем
- Может попросить ввести логин и пароль GitHub (или использовать токен)

**Если просит пароль:**
- Используйте **Personal Access Token** вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите срок действия и права: `repo` (полный доступ к репозиториям)
- Скопируйте токен и используйте его как пароль

### Шаг 9: Проверка

1. Зайдите на ваш репозиторий на GitHub
2. Вы должны увидеть все ваши файлы!
3. ✅ Готово!

---

## 🔄 Часть 2: Как обновлять код на GitHub

### Вариант A: Обновление через командную строку (рекомендуется)

#### Шаг 1: Проверьте текущие изменения

```bash
cd C:\Users\boiar\tower_bot
git status
```

Покажет, какие файлы изменены.

#### Шаг 2: Добавьте измененные файлы

**Добавить все изменения:**
```bash
git add .
```

**Или добавить конкретные файлы:**
```bash
git add bot.py database.py
```

#### Шаг 3: Сделайте коммит (сохранение)

```bash
git commit -m "Описание изменений"
```

Примеры сообщений:
- `git commit -m "Fix Python version compatibility"`
- `git commit -m "Add new feature: deposit ready button"`
- `git commit -m "Update bot functionality"`

**Что такое коммит:**
- Это "снимок" вашего кода в определенный момент
- Можно вернуться к любому коммиту в будущем
- Сообщение должно описывать, что изменилось

#### Шаг 4: Загрузите на GitHub

```bash
git push
```

Готово! Изменения загружены на GitHub.

#### Шаг 5: Railway автоматически обновит

- Railway автоматически обнаружит изменения на GitHub
- Начнет новый деплой
- Пересоберет и перезапустит бота

### Вариант B: Обновление через GitHub Desktop (графический интерфейс)

1. Скачайте GitHub Desktop: https://desktop.github.com
2. Установите и войдите в GitHub
3. Добавьте ваш репозиторий: **File** → **Add local repository**
4. Выберите папку `C:\Users\boiar\tower_bot`
5. При изменении файлов:
   - В левой панели увидите изменения
   - Введите описание изменений
   - Нажмите **"Commit to main"**
   - Нажмите **"Push origin"**

### Полный пример обновления:

```bash
# 1. Перейдите в папку проекта
cd C:\Users\boiar\tower_bot

# 2. Проверьте изменения
git status

# 3. Добавьте все изменения
git add .

# 4. Сохраните изменения (коммит)
git commit -m "Fix Python version - use 3.11 instead of 3.13"

# 5. Загрузите на GitHub
git push

# 6. Готово! Railway обновит бота автоматически
```

---

## 📋 Часть 3: Полезные команды Git

### Просмотр статуса:
```bash
git status                    # Что изменено
git log                       # История коммитов
git log --oneline            # История в одну строку
```

### Отмена изменений:
```bash
git restore файл.py          # Отменить изменения в файле (до коммита)
git restore .                # Отменить все изменения (до коммита)
```

### Просмотр изменений:
```bash
git diff                     # Показать что изменилось
git diff файл.py             # Изменения в конкретном файле
```

### Синхронизация с GitHub:
```bash
git pull                     # Скачать изменения с GitHub (если правили на сайте)
git fetch                    # Проверить есть ли обновления (не скачивать)
```

---

## 🎯 Часть 4: Типичные сценарии

### Сценарий 1: Исправили баг в bot.py

```bash
# 1. Исправили файл bot.py в редакторе

# 2. Добавили изменения
git add bot.py

# 3. Сохранили
git commit -m "Fix bug in bot.py"

# 4. Загрузили
git push
```

### Сценарий 2: Добавили новую функцию

```bash
# 1. Добавили новый файл или изменили несколько файлов

# 2. Добавили все изменения
git add .

# 3. Сохранили
git commit -m "Add new feature: deposit ready button"

# 4. Загрузили
git push
```

### Сценарий 3: Обновили зависимости

```bash
# 1. Изменили requirements.txt

# 2. Добавили изменения
git add requirements.txt

# 3. Сохранили
git commit -m "Update dependencies: use Python 3.11"

# 4. Загрузили
git push
```

### Сценарий 4: Откат к предыдущей версии (если что-то сломалось)

```bash
# 1. Посмотреть историю
git log --oneline

# 2. Скопировать хэш коммита (первые символы)

# 3. Вернуться к коммиту
git checkout ХЭШ_КОММИТА

# 4. Создать новую ветку (опционально)
git checkout -b fix-version

# 5. Вернуться к main
git checkout main
```

---

## ⚠️ Часть 5: Частые проблемы и решения

### Проблема 1: "Permission denied"

**Решение:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

### Проблема 2: "Nothing to commit"

**Решение:**
- Это значит, что нет изменений
- Проверьте `git status`

### Проблема 3: "Please tell me who you are"

**Решение:**
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

### Проблема 4: "Remote origin already exists"

**Решение:**
- Репозиторий уже подключен
- Можно использовать: `git remote set-url origin НОВЫЙ_URL`

### Проблема 5: Не загружаются файлы из .gitignore

**Решение:**
- Это правильно! Файлы в .gitignore НЕ должны загружаться
- Например: `bot.db`, `__pycache__/`, `.env`

---

## 📁 Часть 6: Структура репозитория на GitHub

### Что должно быть на GitHub:

✅ **ОБЯЗАТЕЛЬНО:**
- `bot.py` - основной файл
- `database.py` - работа с БД
- `config.py` - конфигурация (БЕЗ токена!)
- `requirements.txt` - зависимости
- `README.md` - описание
- `.gitignore` - исключения
- `runtime.txt` - версия Python
- `railway.json` - настройки Railway

✅ **ДОКУМЕНТАЦИЯ:**
- `RAILWAY_SETUP.md` - инструкция по Railway
- `RENDER_DEPLOY.md` - инструкция по Render
- `DEPLOY.md` - общая инструкция
- `GITHUB_COMPLETE_GUIDE.md` - эта инструкция

❌ **НЕ ДОЛЖНО БЫТЬ:**
- `bot.db` - база данных
- `__pycache__/` - кэш Python
- `test_bot.py` - тестовые файлы
- `.env` - секреты
- `START_BOT.bat` - локальные скрипты

---

## 🎉 Часть 7: Чеклист первой загрузки

- [ ] Git установлен
- [ ] Репозиторий создан на GitHub
- [ ] Git настроен (имя и email)
- [ ] Репозиторий инициализирован (`git init`)
- [ ] Файлы добавлены (`git add .`)
- [ ] Первый коммит сделан (`git commit`)
- [ ] GitHub подключен (`git remote add origin`)
- [ ] Код загружен (`git push`)
- [ ] Файлы видны на GitHub
- [ ] Переменная `BOT_TOKEN` добавлена на Railway

---

## 🔄 Часть 8: Чеклист обновления кода

- [ ] Изменения внесены в файлы
- [ ] Изменения добавлены (`git add .`)
- [ ] Коммит создан (`git commit -m "..."`)
- [ ] Код загружен (`git push`)
- [ ] Railway начал новый деплой
- [ ] Логи проверены на ошибки
- [ ] Бот работает после обновления

---

## 💡 Советы:

1. **Коммитьте часто** - каждое логическое изменение отдельным коммитом
2. **Хорошие сообщения коммитов:**
   - ✅ "Fix Python 3.13 compatibility"
   - ✅ "Add deposit ready button"
   - ❌ "fix" или "update" (слишком общее)
3. **Проверяйте `git status`** перед коммитом
4. **Используйте `.gitignore`** - не коммитьте секреты!
5. **Делайте `git push`** регулярно - чтобы не потерять код

---

## 📞 Помощь:

Если что-то не работает:
1. Проверьте `git status` - что показывает?
2. Проверьте `.gitignore` - может файл игнорируется?
3. Проверьте логин/пароль GitHub
4. Посмотрите ошибку в терминале - она обычно понятная

**Готово! Теперь вы знаете как работать с GitHub!** 🎉
