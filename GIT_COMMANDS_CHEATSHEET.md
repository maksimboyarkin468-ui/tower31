# 📝 Шпаргалка по Git командам

## 🚀 Первая настройка (один раз):

```bash
# Настройка Git
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Инициализация репозитория
git init

# Подключение к GitHub
git remote add origin https://github.com/yourusername/tower-bot.git

# Создание основной ветки
git branch -M main
```

---

## 📤 Первая загрузка:

```bash
# Добавить все файлы
git add .

# Сохранить изменения
git commit -m "Initial commit: Tower Bot"

# Загрузить на GitHub
git push -u origin main
```

---

## 🔄 Обновление кода (каждый раз):

```bash
# 1. Проверить что изменилось
git status

# 2. Добавить изменения
git add .

# 3. Сохранить
git commit -m "Описание изменений"

# 4. Загрузить на GitHub
git push
```

---

## 🔍 Полезные команды:

```bash
# Просмотр изменений
git status              # Что изменено
git diff                # Что конкретно изменилось
git log                 # История коммитов
git log --oneline       # История в одну строку

# Отмена изменений
git restore файл.py     # Отменить изменения в файле
git restore .           # Отменить все изменения

# Синхронизация
git pull                # Скачать изменения с GitHub
git fetch               # Проверить обновления (не скачивать)
```

---

## 📋 Быстрые команды для обновления:

### Полная команда в одну строку:
```bash
git add . && git commit -m "Описание" && git push
```

### Примеры:
```bash
# Исправили баг
git add . && git commit -m "Fix bug" && git push

# Добавили функцию
git add . && git commit -m "Add new feature" && git push

# Обновили зависимости
git add requirements.txt && git commit -m "Update dependencies" && git push
```

---

## ⚠️ Если что-то пошло не так:

```bash
# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалив изменения)
git reset --hard HEAD~1

# Изменить последнее сообщение коммита
git commit --amend -m "Новое сообщение"

# Удалить файл из Git (но оставить локально)
git rm --cached файл.py
```

---

## 🎯 Типичные сценарии:

### Сценарий: Исправил код → Обновил на GitHub → Railway перезапустил

1. Отредактировал `bot.py`
2. `git add .`
3. `git commit -m "Fix code"`
4. `git push`
5. ✅ Railway автоматически обновит бота!

### Сценарий: Добавил новую функцию

1. Создал/изменил файлы
2. `git add .`
3. `git commit -m "Add deposit ready feature"`
4. `git push`
5. ✅ Готово!

### Сценарий: Обновил зависимости

1. Изменил `requirements.txt`
2. `git add requirements.txt`
3. `git commit -m "Update to Python 3.11"`
4. `git push`
5. ✅ Railway переустановит зависимости!

---

## 💡 Запомните:

- **`git add .`** = подготовить изменения
- **`git commit -m "..."`** = сохранить изменения
- **`git push`** = загрузить на GitHub
- **Railway автоматически** обновит бота после push!
